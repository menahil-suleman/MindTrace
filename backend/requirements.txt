fastapi==0.111.0
uvicorn[standard]==0.30.1
groq==0.9.0
sqlalchemy==2.0.51
asyncpg==0.31.0
alembic==1.13.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-dotenv==1.0.1
email-validator==2.1.1

# RAG dependencies
# sentence-transformers: loads the HuggingFace embedding model locally
# No API key needed, no cost, runs inside our Python process
# all-MiniLM-L6-v2 produces 384-dimensional vectors — small, fast, good enough
sentence-transformers==3.0.1
# pgvector: Python client that teaches SQLAlchemy how to store/query vectors
pgvector==0.3.2
# rank-bm25: keyword search (BM25 algorithm) — the other half of hybrid search
rank-bm25==0.2.2

# Testing (used by CI)
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
aiosqlite==0.20.0